// An exitcode marker is sufficient to create a runtime test; output is diagnostic payload.
//?exitcode(7)

function printf(fmt : ^char, ...) -> int [[external]]

function *Main() -> int:
    printf("exitcode-only stdout is diagnostic payload\n")
    return 7
endfunc
