use rtl/rtlint

#include header

func printf(fmt : ^char, ...) -> int [[external]]

func *Main() -> int:
    printf("header value = %d\n", HEADER_VALUE) //?check("header value", 36)
    return 0
endfunc
