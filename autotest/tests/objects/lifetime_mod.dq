//?notest

use libc/stdio;

object OModChild:
  id : int;

  function *Create(a : int):
    id = a;
    @.printf("modchild.create = %d\n", id);
  endfunc

  function *Destroy():
    @.printf("modchild.destroy = %d\n", id);
  endfunc
endobj
