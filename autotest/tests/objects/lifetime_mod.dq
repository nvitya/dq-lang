//?notest

use libc/stdio

object OModChild:
    id : int

    func *Create(a : int):
        id = a
        @.printf("modchild.create = %d\n", id)
    endfunc

    func *Destroy():
        @.printf("modchild.destroy = %d\n", id)
    endfunc
endobject
