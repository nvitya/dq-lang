//?exitcode(0)

use dqautotest

var run <- OTestRun()

func CheckTool(name : strview):
    run.RunTool([BinPath(name)])
    run.ExpectExitCode(0)
endfunc

func *Main() -> int:
    CheckTool('dq-inline-asm-test')
    CheckTool('dq-projectfile-test')
    return TestResult()
endfunc
