// dq auto-test file

//#define ERRORTEST

#ifdef ERRORTEST

var i = 1; //?error(TypeSpecExpected)

#else

[[external]] function printf(fmt : ^cchar, ...) -> int;

function main() -> int:
  var strvar : cstring[64] = "abcd";
  printf("Hello\n");      //?check(Hello)
  printf("Hello2=5\n");   //?check(Hello2, 5)
  printf("strvar = \"%s\"\n", strvar);  //?check(strvar,  "abcd")
  printf("strvar2 = %s\n", strvar);     //?check(strvar2, abcd)
  return 0;
endfunc

#endif