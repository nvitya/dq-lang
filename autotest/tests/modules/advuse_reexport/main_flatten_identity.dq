use ./mod_flatten_identity
use ./mod_flatten_leaf as leaf --

func *Main():
    var payload : SSharedPayload = @leaf.MakeShared(41)
    printf("flatten shared identity = %d\n", payload.value) //?check('flatten shared identity', 41)
endfunc
