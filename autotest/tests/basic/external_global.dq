// external global variable import support

function fprintf(stream : pointer, fmt : ^char, ...) -> int [[external("fprintf")]]

var libc_stdout : pointer [[external("stdout")]]

function *Main() -> int:
    fprintf(libc_stdout, "external global = %d\n", 42)  //?check("external global", 42)
    return 0
endfunc
