use print

object OTest:
    name  : str
    m_str : str

    func *Create(aname : str, astr : str) [[overload]]:
        name  = aname
        m_str = astr
    endfunc

    func *Create(aname : str, afmt : str, avalues : []anyvalue) [[overload]]:
        name  = aname
        m_str.AddFmt(afmt, avalues)
    endfunc

    func Print():
        @.PrintLn('{}: {}', [name, m_str])
    endfunc
endobject
