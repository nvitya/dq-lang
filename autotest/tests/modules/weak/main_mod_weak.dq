use ./mod_weak as weakmod
use libc/stdio

func WeakOverride() -> int [[cexport]]:
    return 2
endfunc

func *Main() -> int:
    printf("weak override = %d\n", @weakmod.WeakOverride())  //?check('weak override', 2)
    return 0
endfunc
